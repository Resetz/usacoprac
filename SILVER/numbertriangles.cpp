#include <vector>
#include <iostream>

using namespace std;

vector<vector<int>> v;
int main () {
    int n;
    cin >> n;
    v.resize(n, vector<int>(n));
    for (int i = 1 ; i <= n ; i++) {
        
    }
}