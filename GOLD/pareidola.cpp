#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

inline long longcalc(string s) {
    vector<long long> dp(6);
    for (int i = 0 ; i )
}

int main() {
    string s;
    cin >> s;
    int n;
    cin >> n;
}