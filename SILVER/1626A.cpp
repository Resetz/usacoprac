#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    int n;
    while (n--) {
        string s;
        sort(s.begin(), s.end());
        cout << s << endl; // what
    }
}