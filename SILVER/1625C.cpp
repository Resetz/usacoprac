#include <iostream>
#include <vector>
#include <map>

using namespace std;

int main() {
    int n;

    int low = 0;
    int high = n/2;

    while (high >= low) {
        int mid = (mid + high) / 2;

        cout << "+ mid";
    }
}